function neuralCanvas(id,labels){const canvas=document.getElementById(id),ctx=canvas.getContext('2d');let w,h,nodes=[];const resize=()=>{w=canvas.width=canvas.offsetWidth*devicePixelRatio;h=canvas.height=canvas.offsetHeight*devicePixelRatio;nodes=Array.from({length:id==='heroCanvas'?80:labels.length},(_,i)=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.45,vy:(Math.random()-.5)*.45,label:labels?.[i]}))};resize();addEventListener('resize',resize);function draw(){ctx.clearRect(0,0,w,h);ctx.strokeStyle='rgba(255,255,255,.055)';ctx.lineWidth=devicePixelRatio;for(const a of nodes){a.x+=a.vx*devicePixelRatio;a.y+=a.vy*devicePixelRatio;if(a.x<0||a.x>w)a.vx*=-1;if(a.y<0||a.y>h)a.vy*=-1;for(const b of nodes){const d=Math.hypot(a.x-b.x,a.y-b.y);if(d<170*devicePixelRatio){ctx.globalAlpha=1-d/(170*devicePixelRatio);ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke();}}ctx.globalAlpha=1;const g=ctx.createRadialGradient(a.x,a.y,0,a.x,a.y,12*devicePixelRatio);g.addColorStop(0,'rgba(6,182,212,.9)');g.addColorStop(1,'rgba(124,58,237,0)');ctx.fillStyle=g;ctx.beginPath();ctx.arc(a.x,a.y,12*devicePixelRatio,0,Math.PI*2);ctx.fill();if(a.label){ctx.fillStyle='rgba(250,250,250,.78)';ctx.font=`${13*devicePixelRatio}px Inter, sans-serif`;ctx.fillText(a.label,a.x+14*devicePixelRatio,a.y)}}requestAnimationFrame(draw)}draw()}
document.addEventListener('DOMContentLoaded',()=>{neuralCanvas('heroCanvas');neuralCanvas('researchCanvas',['LLMs','AI Agents','Computer Vision','Deep Learning','Optimization','Reinforcement Learning','Multi-Agent Systems']);});
