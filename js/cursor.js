document.addEventListener('DOMContentLoaded',()=>{const c=document.getElementById('cursor'),f=document.getElementById('cursorFollower'),s=document.getElementById('spotlight');let x=innerWidth/2,y=innerHeight/2,fx=x,fy=y;addEventListener('pointermove',e=>{x=e.clientX;y=e.clientY;c.style.transform=`translate(${x}px,${y}px)`;s.style.left=x+'px';s.style.top=y+'px';},{passive:true});function tick(){fx+=(x-fx)*.14;fy+=(y-fy)*.14;f.style.transform=`translate(${fx}px,${fy}px)`;requestAnimationFrame(tick)}tick();document.querySelectorAll('a,button,input,textarea').forEach(el=>{el.addEventListener('mouseenter',()=>f.style.cssText+=';width:56px;height:56px;background:rgba(255,255,255,.08)');el.addEventListener('mouseleave',()=>f.style.cssText+=';width:34px;height:34px;background:transparent')});});
